__pycache__/
*.py[cod]
.venv/
venv/
env/
build/
dist/
*.egg-info/

# personal, per-machine: launcher bindings, calibrated neutral pose, tuned
# sliders. config.example.json is the committed reference.
projects/airmouse/config.json
projects/airmouse/screenshot.png

# private: real pay data, receipt photos and the Gmail app password.
# This repo is public.
tools/invoice-tracker/data/
tools/invoice-tracker/secrets.json

# his to edit: which merchant maps to which expense category. Regenerates from
# DEFAULT_RULES in receipts.py, so his edits never show up as repo changes.
tools/invoice-tracker/receipt-rules.txt

.vscode/
.idea/
Thumbs.db
desktop.ini
